package main

func Report() {
	/*
		- total emails in folder
		- sender counts to csv file
		- most unread senders (csv)
		- most read senders (csv)
		- [optional] regex sender search
		- [prune] emails with attachments
		  - images vs. pdf/doc
		  - top senders
		  - file sizes
		- [prune] flagged/pinned emails
		  - top senders.
	*/
}
